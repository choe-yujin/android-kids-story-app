<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ (API 31+)용 추가 설정 -->
    <style name="Theme.TaleTail" parent="android:Theme.Material.Light.NoActionBar">
        <!-- 애니메이션 비활성화 -->
        <item name="android:windowAnimationStyle">@null</item>
        
        <!-- 전체화면 설정 -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        
        <!-- 시스템 UI 숨김 -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        
        <!-- Edge-to-edge 지원 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        
        <!-- 로고 배경색 설정 -->
        <item name="android:windowBackground">#FDDF59</item>
        
        <!-- Android 12+ 스플래시 화면 설정 -->
        <item name="android:windowSplashScreenBackground">#FDDF59</item>
        <item name="android:windowSplashScreenIconBackgroundColor">#FDDF59</item>
        <item name="android:windowSplashScreenAnimationDuration">1</item>
    </style>
</resources>
